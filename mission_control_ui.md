I want to change are design to completely match the attached images. I have attached complete dashboard image as well individual more focused images for better implementation.

1. The left sidebar will have all the agents listed. with their name, their role and their status. and clicking on them will open the agent profile model as per attached image. It has it's details, about, skills, tasks and mentions, attention, timeline messages. and send message to that agent section
2. In the middle Mission Queue, add filters like images, all, inbox, assigned, active, review, done, waiting.
   the kanbar column will be Inbox, assigned, InProgress, review, done and Navdeep (with counts in all of them), it will list the tickets that need my review, attention or response. The tickets should have same styling as per the image and clicking anywhere on it should open it's details, no need to have dedicated view more button.
3. and in the left sidebar it will lists all the agents, so as I click on any agent, it will lists all of the tasks it have performed.
4. At the top header, add Active/pause button for changing status, chat, broadcast, Docs.

- Currently to write a task there is a dedicated, input in the header, it will get removed and in the agents list I will click on manager and there in I will drop my task.
- Currently it assings tickets to every agent even though they might not be added. so fix that, it should assign and create tasks particulary to only necessary agent, let other agents be as it is, if their involvement isn't required. and if there is a situation where the assigned agent needs assistance from other agent it will mention them with @name, and they will start collaborating.
- I want a way that each agents work and mention each other with @name, and whenever they need my input they will do @navdeep and it will trigger an notification pop-up. and there will be dedicated listing for the tasks where I got mention so that I can look into. otherwise this agents collaborate like they are professional workers working in a team and replying each other messages and all of that.
- For all the tasks, performed, at the end I should be able to add comment.
